import sys from 'system-components';

const Button = sys({
  is: 'button',
  fontSize: 1,
  fontWeight: 'bold',
  lineHeight: 16 / 14,
  m: 0,
  px: 3,
  py: 2,
  color: 'white',
  bg: 'rgba(0,0,0,0.6)',
  borderRadius: 2,
  border: 0,
});

export default Button;
